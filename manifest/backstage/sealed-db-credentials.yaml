---
# sealed-db-credentials.yaml
#
# ⚠️  THIS FILE IS AUTO-GENERATED — DO NOT EDIT BY HAND ⚠️
#
# This file is the encrypted output of running:
#   bash seal-secret.sh
#
# It was generated by kubeseal using the Sealed Secrets controller's
# public key. Only the Sealed Secrets controller inside your cluster
# can decrypt it back into the real Kubernetes Secret.
#
# To regenerate (e.g. if you need to rotate the password):
#   1. Edit secret.yaml with the new password (locally, never commit)
#   2. bash seal-secret.sh
#   3. git add manifest/backstage/sealed-db-credentials.yaml
#   4. git push — ArgoCD will sync and the controller will re-decrypt
#
# ─────────────────────────────────────────────────────────────────
# PLACEHOLDER — Replace this entire file with the output of seal-secret.sh
# ─────────────────────────────────────────────────────────────────
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: backstage-db-credentials
  namespace: backstage
  annotations:
    # Sync wave 1 — must be decrypted BEFORE cnpg-cluster.yaml (wave 2) runs
    argocd.argoproj.io/sync-wave: "1"
spec:
  encryptedData:
    username: REPLACE_WITH_KUBESEAL_OUTPUT   # ← run seal-secret.sh to generate
    password: REPLACE_WITH_KUBESEAL_OUTPUT   # ← run seal-secret.sh to generate
  template:
    metadata:
      name: backstage-db-credentials
      namespace: backstage
    type: kubernetes.io/basic-auth
