---
# ⚠️  WARNING — THIS FILE MUST NEVER BE COMMITTED TO GIT ⚠️
#
# This is your PLAINTEXT secret template. You use it locally
# with kubeseal to generate "sealed-db-credentials.yaml".
# After sealing, delete or keep this file ONLY on your local machine.
#
# It is protected by .gitignore (secret.yaml is listed there).
#
apiVersion: v1
kind: Secret
metadata:
  name: backstage-db-credentials
  namespace: backstage
type: kubernetes.io/basic-auth
stringData:
  username: backstage
  password: "YourActualStrongPasswordHere!"   # ← EDIT: set your real password here
